import { EmployeeJobStatuses } from './employee-job-statuses';

describe('EmployeeJobStatuses', () => {
  it('should create an instance', () => {
    expect(new EmployeeJobStatuses("")).toBeTruthy();
  });
});
